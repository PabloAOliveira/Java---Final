server:
  port: 8888
spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/PabloAOliveira/Java---Final
          search-paths:
          - microservices-java/configs/{application}
          #username: PabloAOliveira
          #password: senha